run command from service worker console: getAllVisits().then(visits => console.table(visits)) to view the current indexedDB table (visits) in case of betrayal by chrome browser devTools!

TODO: scrape pages for comp sci related keywords. brainstorm most efficeint index sorting for accurate storage of seperate topics to be queried. possible ml integration. 
